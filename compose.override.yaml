# TEMPORARY - development only.
#
# Answers structured generation from fixtures because LOCAL_LLM_BASE_URL
# (192.168.4.114:4000) is unreachable from this machine. Searches run end to end
# through Drive, Sheets and XLSX, but every generated text carries an explicit
# simulated-data marker and no criterion is ever reported as supported.
#
# DELETE THIS FILE once the local model endpoint is reachable again. Docker
# Compose loads it automatically, so leaving it here keeps mock mode silently on.
services:
  agentos-api:
    environment:
      TALENT_MOCK_FALLBACK: "true"
